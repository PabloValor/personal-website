"use strict";

$(document).on('ready', function() {
	
	var $header = $('#header');

	//Set $header height related the viewport height
	$header.height($(window).height());

	console.log('ready :)');	
});


